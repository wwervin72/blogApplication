define([], function () {
    var deps = [];
    var updateArticleModel = angular.module('updateArticle', deps);
    updateArticleModel.controller('updateArticle.ctrl', ['$scope', function($scope){
        
    }]);
    return updateArticleModel;
});